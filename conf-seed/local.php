<?php
/**
 * local.php — benign defaults baked into the image.
 * Security-sensitive settings live in local.protected.php (not editable via web UI).
 */
$conf['title'] = 'DokuWiki';
$conf['lang']  = 'en';
